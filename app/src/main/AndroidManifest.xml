<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nota2banco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nota2Banco">
        <activity android:name=".CLIENTES_ACT"></activity>
        <activity android:name=".PRESTAMOS_ACT" />
        <activity android:name=".SEGURIDAD_ACT" />
        <activity android:name=".MAPS_ACT" />
        <activity android:name=".INFO_ACT" />
        <activity android:name=".HOME_ACT" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>